# PostgreSQL connection for the running application (placeholders only).
# Copy to .env and replace with local values. Do not commit real credentials.

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/<database>
SPRING_DATASOURCE_USERNAME=<username>
SPRING_DATASOURCE_PASSWORD=<password>
